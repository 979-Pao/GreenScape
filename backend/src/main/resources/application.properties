spring.application.name=GreenScape

spring.datasource.url=jdbc:mysql://localhost:3314/store_plants?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=ironhack
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# Hibernate 6 (Spring Boot 3) ? recomienda MySQLDialect, pero mejor dejar que lo detecte solo
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
# Puedes omitir el dialecto; Hibernate lo resuelve. Si lo dejas:
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect


management.endpoints.web.exposure.include=health,info

# spring.jpa.hibernate.ddl-auto=validate
# spring.flyway.enabled=true

# JWT: �fuera de "spring."!
security.jwt.secret=supersecreto-para-demo-que-deberias-cambiar-ya-por-env
security.jwt.expiration-ms=86400000